# @kirejs/assets

`@kirejs/assets` manages and deduplicates style/script assets generated by templates and plugins.

## Purpose

- avoid duplicate asset tags
- centralize generated assets
- support optimized delivery with Kire runtime integration

## Typical Use

Use in apps with many templates/components that inject scripts or styles repeatedly.
This keeps output clean and easier to cache.
