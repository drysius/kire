<?jsc
    const { docs = "not_found" } = $ctx.$params;
    const files = await $ctx.$readdir('./src/markdowns');
    console.log(files);
    const item = files.find(
      p => p.package === package || p.package === '@kirejs/' + package
    );
?>

@dynamicroutes($ctx.packages.map(p => p.package.replace('@kirejs/', '')))

@include('layouts/_main.kire', { title: 'Getting Started' })

@define('content')
    @markdown('markdowns/' + docs + '.md')
@end