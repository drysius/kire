<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ it.title || 'Kire Deferred Stream' }}</title>
    <style>
        body { font-family: sans-serif; line-height: 1.6; max-width: 800px; margin: 2rem auto; padding: 0 1rem; background: #f4f4f9; }
        .chunk { border-left: 4px solid #28a745; padding: 0.5rem 1rem; margin: 1rem 0; background: #fff; border-radius: 0 4px 4px 0; animation: fadeIn 0.5s ease-out; }
        .placeholder { color: #888; font-style: italic; }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
    </style>
</head>
<body>
    <header>
        <h1>{{ it.title }}</h1>
        <p>This page loads the main layout instantly, then streams deferred content out-of-order!</p>
    </header>
    
    <main>
        @yield('default')
    </main>

    <footer>
        <hr>
        <p>&copy; 2026 Kire Deferred Example</p>
    </footer>
</body>
</html>
